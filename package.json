{
  "name": "@phetaibtc/nest-auth",
  "version": "0.0.1",
  "description": "Reusable NestJS Auth Module",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json"
  },
  "peerDependencies": {
    "@nestjs/common": "^10 || ^11",
    "@nestjs/core": "^10 || ^11",
    "@nestjs/jwt": "^10 || ^11",
    "@nestjs/passport": "^10 || ^11",
    "passport": "^0.7.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "passport-jwt": "^4.0.1",
    "rxjs": "^7.8.2"
  }
}